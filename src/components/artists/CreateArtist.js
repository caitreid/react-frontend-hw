// CreateArtist needs to render a form
// that form should build a artist object in state
// the form should make an axios post request when submitted
// we should send an alert upon success or failure
// on success: component should redirect our user to the new artist show page
// on failure: component should send the message and remain visible

const CreateArtist = (props) => {
    return <p>create artist page</p>
}

export default CreateArtist